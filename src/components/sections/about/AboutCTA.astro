<!-- ============================================ --><!--                     CTA                      --><!-- ============================================ -->
<section id="cta-1693">
  <div class="cs-container">
    <div class="cs-content">
      <p class="cs-title">
        We're not just giving  The tools. We're giving you the platform to make something extraordinary.
      </p>
    </div>
  </div>
</section>

<style>
  /*-- -------------------------- -->
<---            CTA             -->
<--- -------------------------- -*/

  /* Mobile - 360px */
  @media only screen and (min-width: 0rem) {
    #cta-1693 {
      padding: var(--sectionPadding);
      background-image: url("/regenerative_tourism_bg.png");
      background-size: cover; /* Ensures the image covers the whole area */
      background-position: top; /* Centers the image */
      background-repeat: no-repeat; /* Prevents the image from repeating */
      position: relative;
      overflow: hidden;
    }
    #cta-1693 .cs-container {
      width: 100%;
      max-width: 80rem;
      margin: auto;
      display: flex;
      flex-direction: column;
      align-items: center;
      /* 48px - 64px */
      gap: clamp(3rem, 6vw, 4rem);
    }
    #cta-1693 .cs-content {
      /* set text align to left if content needs to be left aligned */
      text-align: center;
      width: 100%;
      gap: 1.5rem;
    }
    
#cta-1693::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5); /* Adjust the opacity to your liking */
  z-index: 1; /* Make sure it's on top of the background but behind the content */
}

#cta-1693 .cs-container,
#cta-1693 .cs-content {
  position: relative;
  z-index: 2; /* Ensure content appears above the overlay */
}
    #cta-1693 .cs-title {
      /* max-width: 30ch; */
      margin: 0;
      color: var(--bodyTextColorWhite);
      text-transform: uppercase;
    }
    #cta-1693 .cs-text {
      color: var(--bodyTextColorWhite);
      text-align: center;
    }
  
  }
  /* Tablet - 768px */
  @media only screen and (min-width: 48rem) {
    #cta-1693 .cs-content {
      text-align: center;
      padding: 100px 0;
    }
    .cs-title{
      font-size: 2.5rem;
      max-width: 80rem;
    }
  }
</style>

